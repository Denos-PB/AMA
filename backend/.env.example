# AMA Social Media Agent Configuration

# ============================================================================
# MODEL CONFIGURATION
# ============================================================================

# Main LLM model for text enhancement, audio scripts, descriptions
WRITER_MODEL=gemini-2.0-flash

# Audio generation engine
AUDIO_ENGINE=edge-tts

# Image generation service
IMAGE_MODEL=pollinations

# Video generation (disabled by default to save costs)
VIDEO_MODEL=disabled

# ============================================================================
# GENERATION PARAMETERS
# ============================================================================

# Maximum retry attempts for failed worker processes
MAX_RETRIES=3

# Maximum words for audio scripts to keep audio/video concise
SCRIPT_MAX_WORDS=120

# Default TTS voice for audio generation
DEFAULT_VOICE=neutral-female

# Fallback image style when none provided by user
IMAGE_STYLE=cinematic

# Video duration hint for video generation
VIDEO_DURATION_HINT=short

# ============================================================================
# FILE SYSTEM CONFIGURATION
# ============================================================================

# Root directory for generated assets (audio, images, etc.)
# Will create subdirectories: outputs/audio, outputs/images
OUTPUT_DIR=outputs

# ============================================================================
# LOGGING CONFIGURATION
# ============================================================================

# Logging level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# Enable verbose logging (logs to both console and file)
VERBOSE=false

# ============================================================================
# API CONFIGURATION
# ============================================================================

# FastAPI host
API_HOST=0.0.0.0

# FastAPI port
API_PORT=8000

# Enable CORS (for frontend access)
CORS_ENABLED=true

# CORS allowed origins (comma-separated)
CORS_ORIGINS=http://localhost:3000,http://localhost:8080

# ============================================================================
# API KEYS & SECRETS
# ============================================================================

# Google Generative AI API Key (required)
GOOGLE_API_KEY=your_google_api_key_here

# Optional: Add any other API keys needed for external services
# POLLINATIONS_API_KEY=your_key_here

# ============================================================================
# DATABASE (Optional, if needed in future)
# ============================================================================

# DATABASE_URL=sqlite:///ama.db

# ============================================================================
# NOTES
# ============================================================================
# - Ensure all required keys are filled before running the application
# - Do NOT commit .env file to version control
# - For production, use secure environment variable management
# - Check documentation for latest available models
