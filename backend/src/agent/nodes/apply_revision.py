from src.agent.state import OverallState


async def apply_revision_node(state: OverallState) -> dict:
    # Stub: not on main path until POST /chat is wired.
    return {
        "completed_steps": ["apply_revision"],
    }
